{
  "license": {
    "status": "expired",
    "type": "enterprise",
    "issue_date": "{{now offset='-33 days' timezone='UTC'}}",
    "expiry_date": "{{now offset='-3 days' timezone='UTC'}}",
    "max_nodes": 10,
    "issued_to": "tester",
    "issuer": "test-fixture"
  }
}