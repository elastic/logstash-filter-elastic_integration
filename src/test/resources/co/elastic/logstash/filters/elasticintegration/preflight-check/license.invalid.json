{
  "license": {
    "status": "invalid",
    "type": "enterprise",
    "issue_date": "{{now offset='100 days'  timezone='UTC'}}",
    "expiry_date": "{{now offset='1000 days' timezone='UTC'}}",
    "max_nodes": 10,
    "issued_to": "tester",
    "issuer": "test-fixture"
  }
}